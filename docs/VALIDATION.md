# 干净快照验证

验证日期：2026-09-20。

在当前工作目录完成脱敏后运行 `pnpm check`：

- 生成 ABI/schema 漂移检查、全部包构建和 TypeScript 检查通过。
- 21 项 Foundry 测试通过。
- 58 项 Node 测试通过（包含 R4 外部接单、R5 托管执行、免费模型限制与 fallback、QuickNode 通知、轮询调度及 R6 市场/通知/故障恢复）。
- 独立 CLI/MCP 打包成功，在仓库之外启动并发现 9 个标准 MCP 工具；已撤销执行器凭证被拒绝。
- 公开 Markdown 的本地链接无缺失。

原始日志留在维护者忽略的工作目录，避免带入绝对路径与环境元数据。此检查不广播 Testnet 交易，也不等同于完整 Human 钱包或生产安全审计。

发布前检查涵盖已跟踪文件类型、项目实际凭证的精确值、个人路径/标识、真实 provider 配置，以及最终 Git 对象与归档清单。公开测试中的虚构凭证样例和官方依赖字节码保留。后续变更仍须重新审查，不能以本次结果保证未来提交安全。

R5 另完成两位 Testnet 用户的浏览器托管任务：研究及 Transfer 分析均获独立审核接受并付款，执行时间重叠；浏览器使用受限软件钱包 fixture，实际计算在云端进行。桌面与 390px 手机截图已查看，沿用既有字体、配色、导航和卡片，手机无横向溢出。以上不代替真实扩展钱包或独立设备 Human 验证。

资源优化已完成真实 QuickNode RPC 和 OpenRouter Nex Mini/Pro 免费模型验证；部署后的小额研究任务完成生成、CF 双阶段审核和用户收款。临时验证入口已移除。可选 webhook 未配置，不能把本地 HMAC 测试等同于真实 QuickNode 投递。

R6 已通过服务端筛选、通知跨账户隔离/跨会话已读及本地链故障矩阵；线上仅验证 API 和专用账户已读状态，没有新建链上任务。桌面和手机 fixture 截图已查看，Esc 关闭与焦点返回通过；真实钱包与独立设备仍待 Human。
