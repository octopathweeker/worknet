# 原工作区

原目标工作区通过自己部署的站点 `/#legacy` 进入，使用访问码连接已有执行环境。默认站点首页为独立 requester 平台，采用钱包签名登录。

本地体验使用 `pnpm demo`，访问码与进程配置写入忽略的 `.runtime/`。旧工作区的 Requester、执行器和模型在本机运行；本机停止后它们也会停止。

独立平台使用 Cloudflare 持续执行，与旧工作区的本机进程分开。部署方式见 [部署说明](DEPLOYMENT.md)，能力边界见 [实现状态](PROJECT-STATUS.md)。真实地址、访问码、钱包和运行日志均不随公开源码提交。
